# Refresh a token

## Overview

The REST API uses OAuth 2.0 for authentication. You can get your access token by following the steps in the [Authorization](../auth.md) section.

## Endpoints

### GET /api/v1/accounts

Returns a list of all accounts.